spacy==3.8.7
spacy_stanza==1.0.4
Unidecode==1.3.8
# Only an indirect dependency (spacy -> typer -> click), but `import spacy`
# needs it, so it is listed to repair environments where it went missing.
click>=8.0
# Pinned: Stanza model loading breaks with newer torch releases. If another
# package pulls in a newer torch, reinstall this version afterwards.
torch==2.5.1
pandas>=1.5
# Only needed to run tests/test_suite.py
pyyaml>=6.0
